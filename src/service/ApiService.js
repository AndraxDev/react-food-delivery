export function getShops() {
    try {
        //const response = // console.log("[API SERVICE] API OK");
        // let data = response.json();
        // let str = data[0].id;
        // // console.log(str.toString());
        // alert(str.toString());
        return "";
    } catch (error) {
        console.log("[API SERVICE] FAILED TO CONNECT TO API");
        return "";
    }
}
